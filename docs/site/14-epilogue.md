# 第14章 行列はどこにいたのか

ここまで、行列を一度も書かずに Transformer を作り、学習し、文章を生成しました。
最後に、「では行列とは何だったのか」を整理します。

## 行列は「まとめて書く記法」

第4章の `Dense` を思い出してください。ニューロンが `out` 個並んでいて、
それぞれが `in` 個の重みで内積を取る。重みは全部で `in × out` 個です。

この `in × out` 個の数を、縦横に並べて 1 つの記号 \( W \) で呼ぶと、
`out` 回の内積が \( Wx \) という 1 つの式になります。それが行列です。

```text
ニューロン 0:  w00*x0 + w01*x1 + w02*x2 + b0      ┐
ニューロン 1:  w10*x0 + w11*x1 + w12*x2 + b1      ├─  まとめて  y = Wx + b
ニューロン 2:  w20*x0 + w21*x1 + w22*x2 + b2      ┘
```

注意も同じです。第7章の「全部の \( i \) について、全部の \( j \le i \) について内積」を
まとめて書くと \( QK^\top \) になります。「未来を見ない」を行列で書くと「マスク行列を足す」になります。

**行列は、ループをまとめて書く記法です。** 意味は変わりません。

## 行列は「まとめて速く計算する手段」

もうひとつの顔があります。GPU は「同じ計算を大量に同時にやる」のが得意です。
`out` 個の内積を 1 つずつ計算する代わりに、全部同時にやれば速い。
行列積のライブラリ（BLAS、cuBLAS）は、この「同時にやる」を極限まで最適化したものです。

このチュートリアルのモデルは、1 ステップに約 0.1 秒かかります。パラメータ約 7,000 個で。
GPT クラスのモデルはパラメータが 1000 億個以上あり、この方式では 1 ステップに数週間かかります。
だから実用では行列を使います。

**行列は、高速化の手段です。** これも、意味は変わりません。

## この本で本当にやったこと

| 世間の言い方 | この本での言い方 | 実体 |
|---|---|---|
| 行列とベクトルの積 \( Wx \) | ニューロンが並んで内積を取る | かけて足す |
| \( QK^\top \) | query と key の似ている度合い | かけて足す |
| マスク行列 | `(0 to i)` | 未来を見ないループ |
| \( \text{softmax}(\cdot) V \) | 割合で value を混ぜる | かけて足す |
| 埋め込み行列と one-hot の積 | 表を引く | 添字アクセス |
| 勾配のヤコビ行列 | 各ノードの局所的な傾き | 連鎖律 |

右の列に、行列は一度も出てきません。
Transformer の中身は「かけて足す」と「割合にする」と「表を引く」でできています。

## それでも行列を学ぶ価値

行列を使わなくても Transformer は理解できる。それがこの本の主張です。
でも、行列を学ぶ価値がないという主張ではありません。

- 論文や他の解説は行列で書かれています。読むには記法を知る必要があります
- 実際にモデルを動かすには、行列ライブラリを使うことになります
- 「ニューロンが並んでいる」を \( W \) と書けると、思考が速くなります

この本を読み終えたあなたなら、\( \text{softmax}(QK^\top / \sqrt{d}) V \) を見て、
「ああ、あのループのことか」と思えるはずです。記法が先にあって意味を後から探すより、
意味が先にあって記法を後から知る方が、ずっと楽です。

## 次にやること

- **コーパスを差し替える**: `src/main/resources/corpus.txt` を好きなテキストにして `sbt "runMain nomatrix.Main train"`
- **モデルを大きくする**: `Corpus.defaultConfig` の `dModel` や `layers` を変えて、損失と速度の変化を見る
- **部品を差し替える**: ReLU を GELU に、位置埋め込みを RoPE に。どれも「数と関数」で書けます
- **速くする**: `Value` を `Double` の配列に置き換えて、行列積を導入する。この本の逆をたどる旅です

## おわりに

このプロジェクトのコードは 700 行ほどで、依存ライブラリは Scala 標準ライブラリだけです。
すべてのテストは `sbt test` で走り、このサイトのコードはすべて mdoc で実行されています。

- リポジトリ: [github.com/kmizu/no-matrix-transformer](https://github.com/kmizu/no-matrix-transformer)
- 質問・誤りの指摘: [Issues](https://github.com/kmizu/no-matrix-transformer/issues)

読んでくださってありがとうございました。
